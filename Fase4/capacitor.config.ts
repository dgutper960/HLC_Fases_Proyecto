import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '2.1_proyecto_ejemplo_firestore',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
